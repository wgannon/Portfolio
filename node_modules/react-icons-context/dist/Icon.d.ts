import { FC } from 'react';
import { ClassesOverride, IconProps } from './types';
declare const useStyles: (props: Partial<IconProps>) => Record<string, string>;
export declare type IconClassesOverrides = ClassesOverride<typeof useStyles>;
interface Props extends Partial<IconProps> {
    classes?: IconClassesOverrides;
}
export declare const Icon: FC<Props>;
export {};
